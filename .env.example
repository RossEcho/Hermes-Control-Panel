# ============================================================
# Hermes Control Panel — Environment Configuration
# Copy this file to .env and fill in values as needed.
# Lines starting with # are comments and are ignored.
# ============================================================

# ------------------------------------------------------------
# Hermes Integration — CLI mode
# Set ENABLE_DIRECT_CLI_MODE=true to use the real Hermes binary.
# ------------------------------------------------------------
HERMES_EXECUTABLE_PATH=
HERMES_HOME=
HERMES_CONFIG_PATH=
HERMES_ENV_PATH=

# ------------------------------------------------------------
# Hermes Integration — API mode
# Set ENABLE_API_MODE=true to use the Hermes REST API.
# ------------------------------------------------------------
HERMES_API_BASE_URL=
HERMES_API_TOKEN=

# ------------------------------------------------------------
# Server
# ------------------------------------------------------------
APP_HOST=localhost
APP_PORT=8080
APP_SESSION_SECRET=change-me-in-production
APP_DEBUG=false

# ------------------------------------------------------------
# Hermes defaults
# ------------------------------------------------------------
DEFAULT_PROFILE=default
DEFAULT_CHAT_MODEL=claude-3-5-sonnet-20241022

# ------------------------------------------------------------
# Mode flags (exactly one should be true)
# ------------------------------------------------------------
ENABLE_MOCK_MODE=true
ENABLE_DIRECT_CLI_MODE=false
ENABLE_API_MODE=false

# ------------------------------------------------------------
# Logging
# ------------------------------------------------------------
LOG_LEVEL=info
